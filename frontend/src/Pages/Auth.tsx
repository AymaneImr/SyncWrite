import React from "react";
import LoginForm from "../Components/LoginForm";

const Auth: React.FC = () => {
  return (
    <div className="auth-page">
      <LoginForm />
    </div>
  );
};

export default Auth;

